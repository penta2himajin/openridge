# Build output
dist/
node_modules/

# Generated by scripts/build-data.ts — treat as build artefacts even though
# they live in git (CLAUDE.md "Architectural Boundaries"). The writer emits
# `JSON.stringify(x, null, 2) + "\n"`, which happens to match Prettier's JSON
# style today, but that is a coincidence rather than a guarantee: if the
# writer ever changes its indent or collapses a structure, format:check would
# fail on main the moment the daily refresh bot commits.
#
# Hand-edited data files (hf_aliases.json, moe_overrides.json,
# manual_params.json, closed_anchors.json) are deliberately NOT ignored —
# those are authored, so keeping them formatted is worth the check.
data/models.json
data/gguf_sizes.json
